import { OAUTH_STATE_COOKIE, encodeOAuthState } from "@shared/const";
export const startLogin = () => { const nonce = crypto.randomUUID(); const redirectUri = `${window.location.origin}/api/oauth/callback`; document.cookie = `${OAUTH_STATE_COOKIE}=${nonce}; Path=/; Max-Age=600; SameSite=None; Secure`; const state = encodeOAuthState({ redirectUri, nonce }); const url = new URL(`${import.meta.env.VITE_OAUTH_PORTAL_URL}/app-auth`); url.searchParams.set("appId", import.meta.env.VITE_APP_ID); url.searchParams.set("redirectUri", redirectUri); url.searchParams.set("state", state); url.searchParams.set("type", "signIn"); window.location.href = url.toString(); };
